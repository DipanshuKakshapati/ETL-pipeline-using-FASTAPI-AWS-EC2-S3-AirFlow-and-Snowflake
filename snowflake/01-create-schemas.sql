-- Create the landing_zone schema
CREATE OR REPLACE SCHEMA landing_zone;
-- Create the curated_zone schema
CREATE OR REPLACE SCHEMA curated_zone;
-- Create the consumption_zone schema
CREATE OR REPLACE SCHEMA consumption_zone;

-- Display a list of all schemas in the current database
SHOW SCHEMAS;
